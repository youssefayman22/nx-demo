{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "id": "app-generator",
  "title": "Create Team Application",
  "description": "Generate a React+Vite application with feature-based architecture and governance tags",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Application name (kebab-case)",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What name would you like to use for the application?"
    },
    "team": {
      "type": "string",
      "description": "Owning team name (e.g., platform, checkout, payments)",
      "x-prompt": "Which team owns this application?"
    },
    "description": {
      "type": "string",
      "description": "Short description of the application",
      "default": ""
    }
  },
  "required": ["name", "team"]
}
